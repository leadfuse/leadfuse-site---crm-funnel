{
  "name": "leadfuse-site-crm-funnel",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "serve . -l tcp://0.0.0.0:${PORT:-3000} --no-clipboard"
  },
  "dependencies": {
    "serve": "^14.2.3"
  }
}